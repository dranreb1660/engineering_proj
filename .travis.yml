sudo: required

services:
  - docker

before_install:
  - docker build -t phade160/fpl_app .

script:
  docker run phade160/fpl_app 

after_success:
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID"  --password-stdin
  - docker push
